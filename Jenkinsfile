pipeline {
  agent any
  stages {
    stage('Input') {
      steps {
        sh 'truc'
      }
    }
    
    stage('Configuration initiale') {
      steps {
        sh 'bash setup.sh'
      }
    }
  }
}
